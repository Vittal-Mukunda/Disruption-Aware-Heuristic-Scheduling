% Pandoc template -> Elsevier elsarticle, for CAOR-D-26-01812.
% Driven by scripts/build_submission.py.
%
% CAOR Guide for Authors (ScienceDirect, accessed 2026-08-19):
%   - single-anonymized review
%   - Harvard (author-year) bibliography
%   - highlights: 3-5 bullets, <=85 characters each (also in paper/highlights.txt)
%   - abstract <= 250 words
%   - keywords: 1-7
%   - data statement required (Option C)
%   - CRediT required
%   - no generative-AI declaration in this source (authors will add at submission)
%
% The `review` option is Elsevier's double-spaced single-column review layout.
% Swap to `final,5p,times,twocolumn` for a preprint that looks like the article.
\documentclass[review,authoryear,3p,times]{elsarticle}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=black,citecolor=black,urlcolor=blue}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\pandocbounded}[1]{#1}

\newtheorem{proposition}{Proposition}
\newtheorem{assumption}{Assumption}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\journal{Computers \& Operations Research}

\begin{document}

\begin{frontmatter}

\title{$title$}

$for(author)$
\author[rvce]{$author.name$$if(author.corresponding)$\corref{cor1}$endif$}
$if(author.email)$\ead{$author.email$}$endif$
$endfor$

\affiliation[rvce]{organization={Department of Industrial Engineering and Management, R. V. College of Engineering},
                   addressline={Mysore Road},
                   city={Bengaluru},
                   postcode={560059},
                   country={India}}

\cortext[cor1]{Corresponding author.}

\begin{abstract}
$abstract$
\end{abstract}

\begin{highlights}
\item Rollout labels beat FQI; PPO needs obs/reward normalisation.
\item DAHS costs 381 vs Always-COVERT 454; teachers remain cheaper.
\item Labels from M=1 to M=40 match on deployed cost.
\item Distillation is 176 times faster than online truncated lookahead.
\item PPO 78\% gap-closed is a test-scored HP cell, not calib.
\end{highlights}

$if(keywords)$
\begin{keyword}
$for(keywords)$$keywords$$sep$ \sep $endfor$
\end{keyword}
$endif$

\end{frontmatter}

$body$

\end{document}
